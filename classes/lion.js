class Lion {
    #id
    constructor(name, age, id) {
        this.name = name;
        this.age = age;
        this.#id = id;
    }

    // Getters || Setters || Manipulators

    // Methods
}

module.exports = {
    Lion,
}